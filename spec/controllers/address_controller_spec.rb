require 'rails_helper'
RSpec.describe AddressController, type: :controller do
 # let(:user) {FactoryGirl.create :user, guest: false}
#  let(:address) { FactoryGirl.create(:billing_address) }
 # let(:valid_attrs) { FactoryGirl.attributes_for :billing_address }
  describe "PUT #update" do
    #before :each do
     # @billing_address = Factory(:billing_address, firstname: "Lawrence", lastname: "Smith")
    #end 
    
  #  before do
   #   allow(controller).to receive(:current_user).and_return user
    #end
    
    #context "with valid attributes" do
     # it "saves the updating address in the database" do
      #  expect_any_instance_of(Address).to receive(:update)
       # put :update, {id: address.to_param, address: valid_attrs }
    #  end
        
     # it "redirects to the home page"
  #  end
    
   # context "with invalid attributes" do
    #  it "does not save the updating address in the database"
    #  it "re-renders the :new template"
  #  end
  end
  
end