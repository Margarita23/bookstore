<div class="checkout">
  <div class="back_orders">
    <%= link_to t(:to_orders), orders_path %>
  </div>
  <div class="head_of_order"><%= t(:order)%> <%= @order.number %></div><div class="delivery_meth"><%= @order.state %></div> 
  <table class="cart">
    <th><%= t(:book) %></th>
    <th><%= t(:price) %></th>
    <th><%= t(:qty) %></th>
    <th><%= t(:total) %></th>
    <%= render "orders_items", collection: @order.line_items %>
  </table>
  <div class="order_total">
    <div>
      <%= t(:subtotal)%>: $ <%= @order.order_sub_total %>
    </div>
    <div>
      <%= t(:discount)%>: <%= @order.coupon.nil? ? 0 : @order.coupon.discount %> %
    </div>
    <div>
      <%= t(:total)%> : $ <%= @order.total_price - @order.delivery.price %>
    </div>
    <div>
     <%= t(:shipping) %>: $ <%= @order.delivery.price %>
    </div>
    <div>
      <%= t(:ord_total) %>: $ <%= @order.total_price %>
    </div>
  </div>
  <div class="dotted"></div>
</div>